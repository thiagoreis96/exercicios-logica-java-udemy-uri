package e_matrizes;

import java.util.Scanner;

public class Mt07 {
	
	public static void main(String[] args) {
		
		Scanner sc = new Scanner(System.in);
		
		int M = sc.nextInt();
		int N = sc.nextInt();
		
		double[][] valores = new double[M][N];
		
		for (int i=0; i<=N; i++) {
			for (int j=; j<=N; j++) {
				valores[i][j] = sc.nextDouble();
			}
		}
		
		int linha = sc.nextInt();
		
		for (int j=1; j<=M; j++) {
			valores[linha][j] = valores[linha + 1][j];
			System.out.println(valores[linha][j]);
		}
		
		sc.close();
	}
}